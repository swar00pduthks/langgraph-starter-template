{{- if .Values.podDisruptionBudget.enabled }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "langgraph-app.fullname" . }}
  labels:
    {{- include "langgraph-app.labels" . | nindent 4 }}
spec:
  minAvailable: {{ .Values.podDisruptionBudget.minAvailable }}
  selector:
    matchLabels:
      {{- include "langgraph-app.selectorLabels" . | nindent 6 }}
{{- end }}
