apiVersion: v1
kind: Service
metadata:
  name: {{ include "langgraph-app.fullname" . }}
  labels:
    {{- include "langgraph-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "langgraph-app.selectorLabels" . | nindent 4 }}
