apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "langgraph-app.serviceAccountName" . }}
  labels:
    {{- include "langgraph-app.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
